import { Box, Button, Flex, FormControl, FormErrorMessage, Heading, Input } from '@chakra-ui/react'
import React, { useState } from 'react'

const AddCategoryForm: React.FC = (): React.ReactElement => {
	return <Flex></Flex>
}

export default AddCategoryForm
